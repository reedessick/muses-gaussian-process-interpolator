The interpolator can be tested via

```
./test \
    ../etc/equation_of_state.csv.gz \
    --downsample 5 \
    --figtype png \
    --figtype pdf \
    --tag etc-equation_of_state \
    --time-execution
```
